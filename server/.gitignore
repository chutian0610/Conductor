# Build artefacts
bin/
dist/
# Bare built binary (when built at server/conductor via `go build .`)
/conductor

# Go test/coverage
*.test
*.out
coverage.txt

# Editor / OS
.idea/
.vscode/
.DS_Store

# Local agent scratch (created by --cwd / temp MCP configs)
.conductor/

# Coverage artefacts (run `make test-cover` to regenerate)
coverage.*
